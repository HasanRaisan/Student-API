# Student API 🎓

هذا المشروع عبارة عن Web API بسيط لإدارة بيانات الطلاب، تم تطويره باستخدام ASP.NET Core وSQL Server.

## المميزات ✨

- عرض جميع الطلاب
- عرض الطلاب الناجحين فقط
- حساب معدل الدرجات
- البحث عن طالب باستخدام المعرّف
- إضافة طالب جديد
- تحديث بيانات طالب
- حذف طالب

## النقاط التقنية ⚙️

- تم استخدام **Stored Procedures** لجميع عمليات قاعدة البيانات.
- الطبقات مفصولة بشكل واضح:
  - `StudentAPI.Controllers`: طبقة الـ API.
  - `StudentAPIBusiness`: منطق الأعمال (Business Logic).
  - `StudentAPIDataAccess`: الوصول إلى قاعدة البيانات.
  - `StudentAPI.Shared`: يحتوي على DTOs.

## مسارات الـ API 📡

| الطريقة | المسار                        | الوصف                     |
|--------|-------------------------------|---------------------------|
| GET    | `/api/Student/All`            | عرض جميع الطلاب          |
| GET    | `/api/Student/Passed`         | عرض الطلاب الناجحين فقط  |
| GET    | `/api/Student/GradesAverage`  | حساب معدل الدرجات        |
| GET    | `/api/Student/GetByID/{id}`   | البحث عن طالب حسب ID     |
| POST   | `/api/Student/Add`            | إضافة طالب جديد          |
| PUT    | `/api/Student/Update/{id}`    | تحديث بيانات طالب        |
| DELETE | `/api/Student/Delete/{id}`    | حذف طالب                  |

## قاعدة البيانات 🛢️

قاعدة البيانات مستخدمة من نوع SQL Server.
- جميع العمليات تتم عبر **Stored Procedures**.

